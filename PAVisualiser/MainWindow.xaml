<Window x:Class="PAVisualiser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PAVisualiser"
        mc:Ignorable="d"
        Title="Save world viewer" Height="850" Width="800"
        DataContext="MainWindow}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="19*" />
        </Grid.RowDefinitions>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Select" Click="SelectSaveCommand"/>
                <MenuItem Header="_Close" Click="CloseSaveCommand"/>
                <MenuItem Header="_Save"/>
            </MenuItem>
        </Menu>
        <Grid x:Name="worldGrid" Grid.Row="2"></Grid>
    </Grid>
</Window>
